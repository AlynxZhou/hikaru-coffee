// Generated by CoffeeScript 2.3.1
(function() {
  var Generator;

  module.exports = Generator = class Generator {
    constructor(logger) {
      // fn: param page, pages, return Promise
      this.register = this.register.bind(this);
      this.generate = this.generate.bind(this);
      this.logger = logger;
      this.store = {};
    }

    register(layout, fn) {
      if (layout instanceof Object) {
        this.store[layout["layout"]] = layout["fn"];
        return;
      }
      return this.store[layout] = fn;
    }

    generate(page, posts) {
      if (page["layout"] in this.store) {
        return this.store[page["layout"]](page, posts);
      }
      return page;
    }

  };

}).call(this);
